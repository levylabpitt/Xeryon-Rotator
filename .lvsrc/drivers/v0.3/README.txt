Xeryon Labview API

Each axis contains a control screen where you can control this individual axis. 
The "sequencer" should be the main program for the costumer.
Make sure that you configure the script for the correct stage type(s). 

# Static single axis example: 
	an example script to control one stage. Please configure the script to the right stagetype.  

# Static dual axis example: 
	an example script to control one stage. Please configure the script to the right stagetype. 
	
# Dynamic multi axis example
	An example script to control multiple stages. 
	All stages are configured with the correct "config.txt" file in the "Prefrences" folder. 
--> opm van Simon: deze file zou ik niet delen met de klant, geeft teveel verwarring. 